import 'package:flutter/material.dart';

class ScreeningGejalaPage extends StatelessWidget {
  const ScreeningGejalaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screening Gejala')),
      body: Center(child: Text('Form screening gejala kesehatan mental ibu.')),
    );
  }
}
